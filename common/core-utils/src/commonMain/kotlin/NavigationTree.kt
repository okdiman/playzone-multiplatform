object NavigationTree {
    enum class Splash {
        SplashScreen
    }

    enum class Auth {
        AuthFlow, Login, Register, ForgotPassword
    }

    enum class Main {
        Dashboard, Home, Search, Event, Videos
    }

    enum class Home {
        Profile
    }

    enum class Search {
        Game
    }
}